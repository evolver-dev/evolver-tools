# ini_parser package
from .ini_parser import *

# === Auto-registration metadata ===
TOOL_META = {
    "name": "ini-parser",
    "func": "main",
    "desc": 'INI file parser, query, and JSON converter',
}
