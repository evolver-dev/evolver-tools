"""code-auditor — Code analysis: complexity, security, style, deps — CLI+TUI"""
from .cli import main

TOOL_META = {
    "name": "code-auditor",
    "desc": "Code analysis: complexity, security, style, deps — CLI+TUI",
    "func": "main",
}
