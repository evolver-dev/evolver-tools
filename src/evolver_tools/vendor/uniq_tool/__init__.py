# uniq_tool package — re-export
from .uniq import *

# === Auto-registration metadata ===
TOOL_META = {
    "name": "uniq",
    "func": "main",
    "desc": 'Unique line filter with count & case-insensitive',
}
