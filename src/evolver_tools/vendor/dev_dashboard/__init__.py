"""dev-dashboard — Developer dashboard: git, system, ports, processes — CLI+TUI"""
from .cli import main

TOOL_META = {
    "name": "dev-dashboard",
    "desc": "Developer dashboard: git, system, ports, processes — CLI+TUI",
    "func": "main",
}
