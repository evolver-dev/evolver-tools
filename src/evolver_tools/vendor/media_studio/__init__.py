"""media-studio — Media ops: metadata, convert, resize, analyze — CLI+TUI"""
from .cli import main

TOOL_META = {
    "name": "media-studio",
    "desc": "Media ops: metadata, convert, resize, analyze — CLI+TUI",
    "func": "main",
}
