"""config-vault — Secure config: encrypt, vault, env, keyring — CLI+TUI"""
from .cli import main

TOOL_META = {
    "name": "config-vault",
    "desc": "Secure config: encrypt, vault, env, keyring — CLI+TUI",
    "func": "main",
}
