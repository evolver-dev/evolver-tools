# pr_tool package — re-export
from .pr_tool import *

# === Auto-registration metadata ===
TOOL_META = {
    "name": "pr-tool",
    "func": "main",
    "desc": 'GitHub PR helper (create, list, review, checkout)',
}
