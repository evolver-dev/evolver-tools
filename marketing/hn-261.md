# Show HN: evolver-tools — 261 CLI tools, zero dependencies, one pip install

**Title (suggested):**
Show HN: evolver-tools – 261 zero-dependency CLI tools, pip install one get 261

**URL:**
https://github.com/evolver-dev/evolver-tools

---

**Body draft (≈1800 chars — HN limit is 2000):**

I was tired of the "install 20 packages from 5 different package managers" dance every time I set up a new machine. jq, csvkit, ripgrep, httpie, figlet — each is great alone, but together they take minutes and some don't work on Windows.

So I built evolver-tools: one `pip install evolver-tools` gives you 261 CLI tools. Zero external dependencies. Works on Linux, macOS, and Windows.

```
pip install evolver-tools
evtool ascii-banner "Hello HN"     # Large ASCII art
evtool qrcode "https://hn.com"     # QR code in terminal
echo '5,12,8,20,3' | evtool chart-cli --type bar  # Data viz
evtool sysmon                       # Real-time system monitor (curses TUI)
evtool search                       # Browse all 261 tools
```

**How it works (the engineering bit):**

Each tool is a standalone Python module in vendor/ — no imports between them. The CLI runner auto-discovers them at import time using a filesystem scan + TOOL_META registration. Every tool uses ONLY Python standard library — no numpy, no pandas, no requests. The full dependency tree is: Python ≥3.8 and nothing else.

The package is 541KB on PyPI and every tool is `evtool <name> <args>`. Tools use a consistent interface — positional args, flags with `--help`, pipe-friendly stdin/stdout.

**Why 261?**

It's the count of standalone modules auto-discovered at import time. Some are tiny one-liners (rot13, hexdec), some are substantial (sysmon is a full curses dashboard, chart-cli draws bar/line/pie/radar charts, http-server is a static file server). The count grows organically as I encounter needs.

**The honest tradeoffs:**

- Each individual tool is less polished than the dedicated package. jq is way better than `evtool jq-lite`. But you get 261 for the cost of one install.
- "Zero dep" means no syntax highlighting, no binary wheels. Pure Python stdlib only.
- Some tools need network access (geo-ip, crypto-price, weather). They use urllib, which is stdlib.

**What's next:**

- More tools (targeting 300, adding 5-10/week)
- Better cross-platform terminal color support
- Docs site: https://evolver-dev.github.io/evolver-tools/

The project is MIT, free forever. Star if you find it useful: https://github.com/evolver-dev/evolver-tools

What tools would you add?

---

**Also:** The entire project was built by an autonomous AI agent. The story: https://evolver-dev.github.io/evolver-tools/story.html

**Best posting time:** Tuesday-Thursday, 9-11am ET for maximum HN visibility.

**Post tags:** #show #cli #python #devtools
